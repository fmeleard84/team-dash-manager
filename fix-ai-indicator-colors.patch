// Correctifs pour les indicateurs IA - Bleu par défaut, Rouge seulement quand actif

// Dans EnhancedMessageSystemNeon.tsx, remplacer toutes les occurrences de couleur rouge pour l'IA
// par du bleu SAUF quand isAIGenerating === member.id

// 1. Pour les cartes membres dans la sidebar (lignes ~341-347)
// Remplacer:
selectedConversation.id === member.id
  ? member.isAI
    ? "bg-gradient-to-r from-red-500/30 to-red-600/30 border border-red-400/50 shadow-lg shadow-red-500/20"
    : "bg-gradient-to-r from-purple-500/30 to-pink-500/30 border border-purple-400/50 shadow-lg shadow-purple-500/20"
  : member.isAI
    ? "bg-red-500/10 hover:bg-red-500/20 border border-transparent hover:border-red-400/30"
    : "bg-white/5 hover:bg-white/10 border border-transparent hover:border-purple-500/30"

// Par:
selectedConversation.id === member.id
  ? member.isAI && isAIGenerating === member.id
    ? "bg-gradient-to-r from-red-500/30 to-red-600/30 border border-red-400/50 shadow-lg shadow-red-500/20 animate-pulse"
    : member.isAI
    ? "bg-gradient-to-r from-blue-500/30 to-cyan-500/30 border border-blue-400/50 shadow-lg shadow-blue-500/20"
    : "bg-gradient-to-r from-purple-500/30 to-pink-500/30 border border-purple-400/50 shadow-lg shadow-purple-500/20"
  : member.isAI && isAIGenerating === member.id
    ? "bg-red-500/10 hover:bg-red-500/20 border border-transparent hover:border-red-400/30 animate-pulse"
    : member.isAI
    ? "bg-blue-500/10 hover:bg-blue-500/20 border border-transparent hover:border-blue-400/30"
    : "bg-white/5 hover:bg-white/10 border border-transparent hover:border-purple-500/30"

// 2. Pour le badge IA (ligne ~416-417)
// Remplacer:
isAIGenerating === member.id
  ? "bg-gradient-to-r from-red-600 to-red-500 shadow-red-500/50 border border-red-400/50"
  : "bg-gradient-to-r from-red-500 to-red-600 shadow-red-500/30"

// Par:
isAIGenerating === member.id
  ? "bg-gradient-to-r from-red-600 to-red-500 shadow-red-500/50 border border-red-400/50 animate-pulse"
  : "bg-gradient-to-r from-blue-500 to-cyan-500 shadow-blue-500/30"

// 3. Le loader animé reste rouge mais uniquement visible quand isAIGenerating === member.id

// 4. Pour les bulles de message IA (lignes ~525)
// Au lieu d'avoir toujours rouge, ajouter la condition:
{msg.isAI && (
  <div className={cn(
    "flex items-center gap-1 px-1.5 py-0.5 rounded-full",
    isAIGenerating === msg.sender_id
      ? "bg-gradient-to-r from-red-500 to-red-600 animate-pulse"
      : "bg-gradient-to-r from-blue-500 to-cyan-500"
  )}>
    <Bot className="h-3 w-3 text-white" />
    <span className="text-[10px] text-white font-medium">IA</span>
  </div>
)}

// 5. Pour le badge "Génère..." (ligne ~538)
// Remplacer:
isAIGenerating === msg.sender_id
  ? "bg-gradient-to-r from-red-500/20 to-red-600/20 border border-red-400/40 shadow-lg shadow-red-500/20"

// Par:
isAIGenerating === msg.sender_id
  ? "bg-gradient-to-r from-red-500/20 to-red-600/20 border border-red-400/40 shadow-lg shadow-red-500/20 animate-pulse"